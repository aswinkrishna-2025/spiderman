import Link from 'next/link';import {useRouter} from 'next/router';import {Instagram,Mail,Globe2,Menu,X,ShieldCheck,Bug} from 'lucide-react';import {useState} from 'react';import {AnimatePresence,motion} from 'framer-motion';
const links=[['Home','/'],['Prize','/prize'],['Rules','/rules'],['Register','/register'],['Admin','/admin']];
export default function Layout({children}:{children:React.ReactNode}){const [open,setOpen]=useState(false);const r=useRouter();const social={instagram:process.env.NEXT_PUBLIC_INSTAGRAM_URL||'#',email:process.env.NEXT_PUBLIC_CONTACT_EMAIL||'muv@cep.ac.in',website:process.env.NEXT_PUBLIC_WEBSITE_URL||'#'};return <div className="min-h-screen bg-ink"><header className="fixed inset-x-0 top-0 z-50 border-b border-white/10 bg-black/70 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5"><Link href="/" className="flex items-center gap-2"><span className="grid h-9 w-9 place-items-center rounded-full bg-red"><Bug size={19}/></span><span className="font-display text-xl tracking-wider">MuV TEAM</span></Link><nav className="hidden items-center gap-6 md:flex">{links.map(([n,h])=><Link key={h} href={h} className={`flex items-center gap-1 text-sm font-medium transition hover:text-white ${r.pathname===h?'text-red':'text-zinc-400'}`}>{n==='Admin'&&<ShieldCheck size={14}/>} {n}</Link>)}<Link href="/register" className="btn-primary !px-5 !py-2 text-sm">Register now</Link></nav><button className="md:hidden" aria-label="Menu" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div><AnimatePresence>{open&&<motion.nav initial={{height:0}} animate={{height:'auto'}} exit={{height:0}} className="overflow-hidden border-t border-white/10 bg-black md:hidden"><div className="flex flex-col p-5">{links.map(([n,h])=><Link key={h} href={h} onClick={()=>setOpen(false)} className="py-3 text-zinc-300">{n}</Link>)}</div></motion.nav>}</AnimatePresence></header><motion.main key={r.pathname} initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{duration:.35}}>{children}</motion.main><footer className="border-t border-white/10 bg-black px-5 py-10"><div className="mx-auto flex max-w-7xl flex-col items-center justify-between gap-6 md:flex-row"><div><div className="font-display text-xl">MuV TEAM</div><p className="mt-1 text-sm text-zinc-500">College of Engineering Pathanapuram</p></div><div className="flex gap-3"><a aria-label="Instagram" href={social.instagram} className="rounded-full border border-white/10 p-3 hover:bg-white/10"><Instagram size={18}/></a><a aria-label="Email" href={`mailto:${social.email}`} className="rounded-full border border-white/10 p-3 hover:bg-white/10"><Mail size={18}/></a><a aria-label="Website" href={social.website} className="rounded-full border border-white/10 p-3 hover:bg-white/10"><Globe2 size={18}/></a></div></div></footer></div>}
